---
// File: src/layouts/BaseLayout.astro
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="pt" class="bg-slate-50 text-slate-800 antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Porto Não-Monogâmico</title>
  
    <style is:global>
        @import "tailwindcss";
    </style>
  </head>
  <body class="min-h-screen flex flex-col font-sans">
    
    <!-- Top Navigation Bar -->
    <header class="bg-white border-b border-slate-200 sticky top-0 z-50">
      <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
        
        <!-- Site Logo / Brand -->
        <a href="/" class="font-bold text-lg text-indigo-900 hover:text-indigo-600 transition">
          Tertúlias Não Mónó
        </a>

        <!-- Navigation Links -->
            <nav class="flex items-center gap-6 text-sm font-medium text-slate-600">
              <a href="/articles" class="hover:text-indigo-600 transition">Artigos</a>
              <a href="/events" class="hover:text-indigo-600 transition">Eventos</a>
              <a href="/gallery" class="hover:text-indigo-600 transition">Galeria</a>
              <a href="/professionals" class="hover:text-indigo-600 transition">Profissionais</a>
            </nav>
      </div>
    </header>

    <!-- Main Content Container -->
    <main class="flex-grow max-w-5xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-10">
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-slate-200 mt-auto py-8">
      <div class="max-w-5xl mx-auto px-4 text-center text-xs text-slate-500">
        <p>&copy; {new Date().getFullYear()} Tertúlias Não Mónó. Um espaço de diálogo e partilha comunitária.</p>
      </div>
    </footer>

  </body>
</html>